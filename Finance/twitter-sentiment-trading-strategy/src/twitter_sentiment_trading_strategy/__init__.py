"""
twitter_sentiment_trading_strategy package

This file marks the directory as a Python package and provides a central place
for high‑level package documentation.

NOTES:
- This package currently contains the core project code under `src/`.
- The ingestion pipeline lives outside the package for now (in the top‑level
  `ingestion/` folder). If the project grows, the ingestion logic may be moved
  inside this package for cleaner imports and packaging.
- Nothing needs to be imported here yet, but this file can later expose
  top‑level functionality (e.g., version info, common utilities, etc.).

Typical future uses of __init__.py:
- Define package‑wide constants
- Expose a simplified public API
- Handle lazy imports for performance
- Add version metadata (e.g., __version__)
"""

# At this stage, the package doesn't need to expose anything explicitly.
# This file is intentionally minimal.